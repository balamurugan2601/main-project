# Production Environment Variables
# Copy these to your deployment platform (Render, Railway, etc.)

NODE_ENV=production
PORT=5000

# Frontend URL (your Vercel deployment)
FRONTEND_URL=https://main-project-lac.vercel.app

# JWT Configuration
# Generate JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_production_jwt_secret_here
JWT_EXPIRE=30d

# Database Connection
# Get this from your MySQL provider (Aiven, Railway, PlanetScale)
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://user:password@host:port/defcomm
